{
  "name": "marp-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@marp-team/marp-cli": "^2.0.4"
  },
  "scripts": {
      "start": "marp --allow-local-files --pdf-notes --watch hello.md --theme-set arolla.css -o hello.pdf"
  }
}
